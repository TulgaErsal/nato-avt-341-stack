Configuration
=============

This section describes how the stack's parameters are declared, generated and
loaded. Node parameters are defined once in yaml template files; the parameter
library turns those templates into generated C++ and Python interfaces at build
time, and resolves parameter files, launch arguments and command line overrides
at launch time.

.. toctree::
   :maxdepth: 1

   parameter_library.rst
